export default class ShareDBManager {
  moveElement(ele: any) {
    console.log(ele);
  }


  setSizeOfElement(ele: any, newWidth: number, newHeight: number) {
    console.log(ele, newWidth,newHeight);
  }

  // @ts-ignore
  resizeCompound(ele: any, number: number, number2: number, number3: number, number4: number, number5: number, number6: number) {

  }
}
