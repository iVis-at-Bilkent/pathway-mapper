{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "yarn workspace pathway-mapper-standalone build",
    "build:lib": "yarn workspace react-pathway-mapper build",
    "update:lib": "yarn workspace pathway-mapper-standalone upgrade react-pathway-mapper",
    "start": "yarn workspace pathway-mapper-standalone start"
  }
}
